import { createContext } from "react";
//Se crea un contexto para no estar pasando los valores en este caso del login de pagina en pagina o bien de componente en componente
export const Contextt = createContext();
